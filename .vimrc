" Show line numbers.
:set number

" Turn on syntax highlighting.
syntax on

" Allow search with case-insensitive when all characters used in search is in lowercase.
" If any characters used in the search is in uppercase, the search becomes case-sensitive.
set ignorecase
set smartcase

" Enable searching as you type, rather than waiting till enter is pressed.
set incsearch

" Disable audible bell because it's annoying
set noerrorbells visualbell t_vb=
